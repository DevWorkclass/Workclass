/**
 * Login admin. Implémentation à venir (POST /api/admin/auth/login).
 * Aucune logique métier ici — formulaire + appel API.
 */
export default function AdminLoginPage() {
  return (
    <main className="container mx-auto px-4 py-8">
      <h1 className="text-2xl font-bold">Connexion administrateur</h1>
      <p className="mt-2 text-muted-foreground">Squelette — à implémenter.</p>
    </main>
  );
}
