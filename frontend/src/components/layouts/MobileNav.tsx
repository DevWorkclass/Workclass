/**
 * Navigation mobile (bottom bar). Squelette — à implémenter.
 */
export function MobileNav() {
  return (
    <nav
      aria-label="Navigation mobile"
      className="fixed bottom-0 left-0 right-0 border-t md:hidden"
    />
  );
}
