/**
 * Hero d'événement (cover + titre + dates). Squelette — à implémenter.
 */
import type { EventSummary } from '@/domains/public/event/types/event.types';

interface EventHeroProps {
  event: EventSummary;
}

export function EventHero(_props: EventHeroProps) {
  return null;
}
